apiVersion: v1
kind: Secret
metadata:
  name: rails-secrets
  namespace: library-k8s
  labels:
    app.kubernetes.io/name: neighborly-library
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  # IMPORTANT: Replace via kubectl/Helm at deploy time. Do not commit real secrets.
  SECRET_KEY_BASE: "replace-me-with-a-real-secret"
