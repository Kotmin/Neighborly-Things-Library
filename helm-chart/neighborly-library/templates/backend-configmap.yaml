apiVersion: v1
kind: ConfigMap
metadata:
  name: rails-config
  namespace: {{ .Release.Namespace }}
data:
{{- range $k, $v := .Values.backend.env }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
  DATABASE_URL: {{ .Values.backend.storage.databaseUrl | quote }}
